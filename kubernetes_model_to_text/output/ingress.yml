apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: http-ingress
  namespace: uml
spec:
  rules:
  - host: 2dv50e.xyz
    http:
      paths:   
      - path: /bar
       
        backend:
            serviceName: phpmyadmin
            servicePort: 80